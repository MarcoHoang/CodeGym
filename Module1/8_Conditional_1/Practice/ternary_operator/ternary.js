// Exercise 1: Viết chương trình nhập vào giá trị cho a và b. Tính tổng a và b, nếu tổng nhỏ hơn 4, hiển thị chuỗi Below, ngược lại hiển thị Over. Lưu ý sử dụng toán tử ? :

let a = prompt( 'a' );
let b = prompt( 'b' );

sum = ( (a + b) < 4 ) ? 'Below' : 'Over';
console.log(sum);

// Exercise 2: Viết chương trình nhập vào giá trị cho chuỗi message. 

// let login = prompt("Enter:");
// let message = (login == 'Employee') ?
//     'Hello' :
//     (login == 'Director') ?
//       'Greetings' :
//       (login == '') ?
//         'No login' :
//         '';
// console.log(message);